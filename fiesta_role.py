        # Crée un bouton pour créer un ticket
        #fiesta_button = Button(label="Fiesta Rôle", style=discord.ButtonStyle.green, emoji="🍹")

        #async def fiesta_button_callback(interaction):
        #    guild = bot.get_guild(ctx.guild_id)
        #     member = guild.get_member(ctx.user_id)
        #     role_name = "🍹 || Party Notif"
        #     role = discord.utils.get(guild.roles, name=role_name)

        #     if role is not None:
        #         await member.add_roles(role)

          #       channel = bot.get_channel(ctx.channel_id)
            #     message = await channel.fetch_message(ctx.message_id)
              #   await message.remove_reaction('🍹', member)
            # else:
              #   print("Role not found.")

                # await interaction.response.send_message("Role has been give dans")

        # fiesta_button.callback = fiesta_button_callback